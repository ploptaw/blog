---
layout: base
---

<main>
  <article>
    <h1>{{ title }}</h1>
    <time datetime="{{ date }}">{{ date | dateJP }}</time>
    {% if tags %}<span class="tags">{{ tags | join(', ') }}</span>{% endif %}
    <hr class="spacer">
    {{ content | safe }}
  </article>
</main>
