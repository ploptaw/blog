---
layout: base
---

<main>
  <h1>{{ title }}</h1>
  <time datetime="{{ date }}">{{ date | dateJP }}</time>
  {% if tags %}<span class="tags">{{ tags | join(', ') }}</span
  >{% endif %}
  <hr class="span" />
  {{ content | safe }}
</main>
