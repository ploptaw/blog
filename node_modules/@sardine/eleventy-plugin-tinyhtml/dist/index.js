var $6jFYg$htmlminifier = require("html-minifier");


module.exports = (eleventyConfig, options = {
})=>{
    const htmlOptions = {
        collapseBooleanAttributes: true,
        collapseWhitespace: true,
        decodeEntities: true,
        html5: true,
        removeAttributeQuotes: true,
        removeComments: true,
        removeOptionalTags: true,
        sortAttributes: true,
        sortClassName: true
    };
    Object.assign(htmlOptions, options);
    eleventyConfig.namespace('tinyHTML', ()=>{
        eleventyConfig.addTransform('tinyHTML', (content, outputPath)=>{
            try {
                if (outputPath && outputPath.endsWith('.html')) content = $6jFYg$htmlminifier.minify(content, htmlOptions);
            } catch (error) {
                console.error(error);
            }
            return content;
        });
    });
};


//# sourceMappingURL=index.js.map
